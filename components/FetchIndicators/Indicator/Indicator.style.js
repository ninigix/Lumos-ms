import { StyleSheet } from "react-native";

export default StyleSheet.create({
  imageWrapper: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center"
  },
  header: {
    fontSize: 20,
    margin: 20,
    marginBottom: 0
  }
});
